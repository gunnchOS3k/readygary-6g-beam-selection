# What Is Real Today

- sim/ baselines, GNN multi-BS arm, adaptive trackers
- pluggable channels: synthetic, 3GPP TDL/CDL structure, Sionna/AODT adapters (optional)
- `deploy/` HTTP scorer + Docker + ONNX-when-available
- LaTeX paper + CI
- `make smoke` / `make reproduce`
- Docker edge service (`deploy/Dockerfile`)

Smoke: `make smoke`

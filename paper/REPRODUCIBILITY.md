# Reproducibility (Paper II ReadyGary)

```bash
make paper-reproduce
```

Protocol `paper/artifacts/experiment_protocol.yaml` must exist before held-out JSON.

- `SYNTHETIC_SIM` TDL at 28 GHz FR2 (not Sub-6)
- `HOST_PROCESS_TIMING` (not measured RF)
- `sub_ms_inference_proven: false`
